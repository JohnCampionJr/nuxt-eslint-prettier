<script setup lang="ts">
const test = ['j', 'jj', 'jj', 'hjhkj', 'prettier', 'hjhj', 'dfdfd']
const myArray = ['a', 'b', 'c', 'd', 'e'] // note the trailing comma which forces a wrap
function testf(test: string) {
  return test.map((item: any) => `${item}test`)
}
let foo = { bar: 'baz', qux: 'quux' }
const two = clsx('m-5 p-0 focus:bg-amber-300 focus:outline focus:outline-2 focus:outline-offset-2')
const test =
  'shrink-0 flex items-center cursor-pointer select-none px-1 opacity-100 transition-opacity duration-200 hover:text-primary-500'
// Input
export function test(): any {
  return null
}

if (a == true) foo()
else foo()
</script>
{ }
<template>
  <div>
    <div
      class="group m-5 p-0 bg-blend-screen hover:bg-amber-950 focus:bg-amber-300 focus:outline focus:outline-2
        focus:outline-offset-2 focus:outline-amber-500"
    />
    <NuxtRouteAnnouncer />

    <NuxtWelcome />
  </div>
</template>
